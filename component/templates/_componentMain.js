var React = require('react');
const styles = require('./<%= name %>.scss');

var <%= name %> = (props) => {
  return (
    <h1>Your Great <%= name %> component </h1>
  );
}

module.exports = <%= name %>;
